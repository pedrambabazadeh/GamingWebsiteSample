Using childred props make this component reusable. (instead of writing statically, it gets in content based on 
    Box type of the context and wraps it as children)
Also reconsideration in the manner of context usage is require to aquire this.