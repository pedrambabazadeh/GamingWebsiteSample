Impose the SRP (single responsibility principle) to the component.
Divide it to two components one of which returns seceral of the other only!

To make the overflow, we need to use whitespace nowrap and then separately define it for <p>