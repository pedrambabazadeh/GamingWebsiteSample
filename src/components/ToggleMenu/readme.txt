approach has to change to atomic

Apply pollymorphism to be useful for the other buttons too
To do so, we need to create an object in context of menu to store the data as well as the boolean
    of the isOpen. Or, instead of storing the data, indicate the type of the ToggleMenu